spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/highlight_api
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
      generate-ddl: true
      properties:
        hibernate:
          dialect: org:hibernate.dialect.PostgreSQLDialect